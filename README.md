# locaLearn

Local-first terminal study tools.

## Install

```bash
curl -fsSL https://raw.githubusercontent.com/OliverHeffernan/locaLearn/main/install.sh | bash
```

This installer downloads the latest prebuilt release binary for your platform and installs it to `~/.local/bin/loca`.
